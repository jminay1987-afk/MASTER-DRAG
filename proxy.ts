import {createServerClient} from "@supabase/ssr";import {NextResponse,type NextRequest} from "next/server";
export async function proxy(req:NextRequest){let res=NextResponse.next({request:req});const s=createServerClient((process.env.NEXT_PUBLIC_SUPABASE_URL ?? "https://kujcnigznlhsqcrigaru.supabase.co"),(process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY ?? "sb_publishable_bpwtZZplxAZvJaHGG06RLQ_-YJclLB5"),{cookies:{getAll:()=>req.cookies.getAll(),setAll(v){v.forEach(({name,value})=>req.cookies.set(name,value));res=NextResponse.next({request:req});v.forEach(({name,value,options})=>res.cookies.set(name,value,options))}}});const {data}=await s.auth.getClaims();if(req.nextUrl.pathname.startsWith("/admin")&&!data?.claims?.sub){const u=req.nextUrl.clone();u.pathname="/login";return NextResponse.redirect(u)}return res}
export const config={matcher:["/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)"]};
